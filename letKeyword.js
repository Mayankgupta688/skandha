function WorkingWithData() {
    var userName = "Mayank"
    var userSum = 0;
    for(let count = 0; count<100; count++) {
        userSum = userSum + count
    }

    for(let count = 0; count<100; count++) {
        userSum = userSum + count
    }

    for(let count = 0; count<100; count++) {
        userSum = userSum + count
    }

    if(userName == "Mayank") {
        let age = 10;
        console.log(age)
    }

    console.log(userSum);
    console.log(userName)
}

WorkingWithData();